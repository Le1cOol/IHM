<Page
    x:Class="DicoLexique.Accueil"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DicoLexique"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="6*"/>
        </Grid.RowDefinitions>

        <TextBox x:Name="Txt_Search" PlaceholderText="Recherche" Grid.ColumnSpan="2"
                 VerticalAlignment="Stretch" Margin="0,0,0,0"/>
        <Button x:Name="Btn_Search" Content="Rechercher" Grid.Column="2" 
                HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>

        <ListView x:Name="Lv_Histo" Grid.Row="1" Grid.ColumnSpan="3" 
                  HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />
        <RichTextBlock Grid.Row="1" Grid.ColumnSpan="3">
            <Paragraph>
                <Run Text="Définition"/>
            </Paragraph>
        </RichTextBlock>

    </Grid>
</Page>
